name: Release Build

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/reusable-release.yml
    with:
      pages_commit_message: "Deploy to GitHub Pages: ${{ github.sha }}"
    secrets: inherit
